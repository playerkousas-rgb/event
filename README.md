# 🎪 大型活動行政管理工具 (Event Administration Management Tool)

> 參考 `indonesia-trip` 及 `vsbadge` 架構，結合 Google Sheet 與 Google Apps Script (GAS) 遠端控制，支援多活動接入、活動密碼私隱保護、7級角色權限控制與 8大卡片管理模組。

> 📖 **功能介紹及使用說明書**：見 [`docs/FEATURE_GUIDE.md`](docs/FEATURE_GUIDE.md)。APP 內亦提供「功能介紹」按鈕，不登入也可查看（三類卡片、各卡片功能、職級權限制度、可延展功能及 FAQ）。

---

## ✨ 系統核心功能特色

1. **多活動接入首頁 (Multi-Event Landing Page)**：
   - 可以在首頁看到多個不同的大型活動卡片（如：年度週年活動、領袖培訓、冬季大露營）。
2. **活動密碼私隱保護**：
   - 每個活動均可設定專屬密碼。首次接入時輸入密碼後將自動安全儲存於瀏覽器 `localStorage`，保障私隱。
3. **職級權限架構（L1–L7，對用戶顯示）**：
   - **Level 1**: 管理員 (地域 / 新職員 / 行政組)、主席、顧問 (同級，全部可看可管)
   - **Level 2**: 執行副主席 (高層統籌)
   - **Level 3**: 副主席 (各組別，因應活動設立)
   - **Level 4**: 總主任 (各組別，因應活動設立)
   - **Level 5**: 主任 (各組別，因應活動設立)
   - **Level 6**: 工作人員 (Staff)
   - **Level 7**: 公開 (Public，公開資料僅管理員以上可設定)
   - 👁️ **用戶管理可見範圍**：**執行副主席／主席／顧問／管理員**＝可見全部人｜**副主席**＝只睇自己組｜**總主任**＝自己組，唔包括副主席｜**主任**＝自己組普通工作人員。
   - 🚦 **批核權限表**：L1 或以上（管理員／顧問／主席／執行副主席）可查看及修改「批核權限表」，設定各申請範疇的批核組及執行／最後名單組。
   - ⚠️ **開戶權限**：前端開戶卡片只提供 工作人員／主任／總主任 三級；**籌委會架構 54 人已全部預先開戶**（顧問／主席／執行副主席／副主席／總主任／主任／秘書處管理員 5 人，見 `data/isd_2026.json` users + `data/committee_accounts.csv`），冇資料嘅可喺「用戶管理」／開戶表／批量 CSV 補。組別下拉（開戶/登記）不含「顧問團」及「主席及執行副主席」兩組。工作人員（數量多、名單後補）由各組自行開戶。
   - 📇 **部門管理中心 = 10 組**（含 顧問團、主席及執行副主席、秘書處，可查看/統計）。登入後儀表板順序：**公開資料 → 工作卡片（會議卡片最前）→ 管理工具（總主任以上）→ 部門管理中心**；**我的監察**已併入頂部**活動資訊橫幅**（與日期／時間／地點／天氣＋活動簡介＋最新消息同一橫幅；自己＋下級申請批核進度，有權限人士另見總申請／待處理／已批核／已拒絕，可跳轉批核中心）。活動橫幅已精簡：只留 日期/時間/地點/天氣＋活動簡介＋最新消息＋我的監察（大標題／進行中 badge／身份 badge 已刪，活動名稱顯示喺最頂 BAR 標題列）。最頂 BAR（系統名稱標題列）＝身份・**開戶**（總主任以上）・改密碼・登出（登入後不顯示登入，只顯示登出）；下方 BAR＝執行手冊・申請中心・批核中心（登入後有權限人士才可見）・**部門中心**（登入後可見：執副以上一按即見全部部門列表、普通人一按直接跳自己部門，唔使喺儀表板轆頁）；**開戶**已移去最頂 BAR 右側（一按直接跳入開戶頁；儀表板管理工具區「開戶」卡保留，雙入口）；手機底部導覽相同。手機上所有卡片格線自動由長方形開成兩欄正方形，全站防左右滑動。**未登入（一般工作人員／公眾）**＝只有**公告及溝通**及**申請中心**有顏色，其餘卡片無顏色（白底）。物資／泊車證／口頭報價／財務／**車輛** 均不另設獨立卡片，併入現有組別工作卡片（**泊車證沿用車輛申請路由**）。低於總主任的申請先由申請人本組總主任以上確認，再交目前設定的批核組；批核組及執行／最後名單組均可在批核權限頁以按鈕多選並隨時修改。
   - 📢 **公開區（4 張，登入前可看）固定排序**：公告及溝通（**已併入日程表／旅團須知／活動主題章**）→ **執行手冊**（**已併入組織架構與聯絡・場地與活動總覽・典禮儀式（含獲獎名單）・危機處理・財務指引・通告及文件・參加旅團名單・各類附加資料，內部分頁**；v11：**攤位總表**及**場地佈置總覽**已移入「場地與活動總覽」，原「攤位總表」分頁改名**攤位列表**；**箱頭紙・許可證式樣・失物認領**收埋入新分頁「**各類附加資料**」）→ 申請中心 → 童心捐贈大行動（**統計只供服務及發展組／管理層查看**，公眾只可填表／掃碼）。
   - 👑 **典禮儀式卡片已內建官方定稿內容**（2026-10-04，不會再改）：典禮程序（10:45 嘉賓接待 → 11:00 吳家麗會長主禮優異旅團頒發 → 12:00 區永樑指揮官主禮會操檢閱 → 13:00 茶聚 → 14:00 主題活動區）及 嘉賓地圖（東閘入口／警校道／有蓋操場報到處／莫榮大樓接待處／海洋公園停車場／穿梭巴士）已直接內建於卡片，毋須連結 Drive。官方原圖 PDF（`assets/ceremony/guest_map.pdf`）可於「嘉賓地圖」分頁直接檢視／下載。
   - 📖 **會議議程／紀錄「內建 JSON」（免彈出 Google Drive APP）**：議程項目、議決事項及跟進事項已內建於 `data/meeting_records.json`，隨 APP 一齊發佈。會議卡片每張會議下面按「議程（內建·即睇）」／「會議紀錄（內建·即睇）」即喺卡片內展開**摘要／重點**（議程項目、議決事項、跟進事項），唔會跳去 Drive 或彈出 Drive APP；**要睇全文先按「睇全文（頁內開啟）」**（頁內 iframe 開原檔），亦可「下載原檔」或「Drive 開啟 ↗」跳轉（`downloadDriveFile` 用隱藏 iframe 下載，不開新分頁）。管理員可按「內建議程 JSON」直接編輯（存本機即時生效）並匯出覆蓋 `data/meeting_records.json` 發佈；`content_status` 標示「摘要／重點版」或「已錄入全文」。
   - 📁 **會議卡片直接開啟「會議 Drive」（即時連通）**：點入卡片即讀取公開資料夾 `13P0gJ3c-1zXTzniZFZL6VT2EZP_FDTYM`（定義於 `data/isd_2026.json` 的 `drive.groups.會議`／`meeting_upload_folder_id`），按分次子資料夾顯示議程、會議紀錄及附件，並可切換「清單／內嵌」模式。秘書處在 Drive 新增或更新檔案後，重新進入卡片或按「讀取最新」即可同步；GAS `listDriveFolder` 以 `UrlFetchApp` 公開讀取，資料夾及檔案須設為「知道連結的任何人可查看」。
   - 🗑️ **永久刪除申請紀錄**：物資、車輛／泊車及財務紀錄提供授權管理層刪除；刪除會同步至後台，並以本機刪除標記阻止舊快取或同步競態把紀錄重新顯示。後台工作表已刪除的申請亦以後台結果為準，不再被舊 `localStorage` 還原。
4. **8 大卡片管理模組**：
   - 📋 **1. 會議卡片**：會議紀錄、議程安排、各組會議摘要。
   - 👥 **2. 工作人員卡片**：組織架構、職員名單、職務大綱、聯絡資料。
   - 📁 **3. 文件檔案**：通告、保險相關文件與活動檔案。
   - 💰 **4. 財務管理**：活動預算、各組財政報告（參考印尼財務模版）。*已歸入「行政組」卡片，不另設獨立儀表板卡片（指引、預算、開支申報、口頭報價、結算批核均在行政組卡片內處理）。*
   - 🎪 **5. 活動與攤位**：攤位總覽、遊戲卡設計、地圖與活動項目。
   - 🍱 **6. 膳食管理**：登入訂餐、菜單及統計；預設行政組批核、協調組執行及持有最後名單，兩者均可改為多個組別。
   - 📅 **7. 日程表**：全活動時間表、各組行程與時段安排。
   - 📦 **8. 物資管理**：地域物資及攤位計劃書為**兩張完全獨立嘅卡片**（v8.6：「攤位」不再掛喺物資申請卡內——冇頁籤、冇跨卡按鈕、冇混入統計；兩項紀錄各自獨立）。地域物資申請（v8.5 起**不設庫存表**——地域物資逾萬種、攤位物資向外判商租用，毋須對照庫存，原「庫存」分頁已移除）；依批核權限頁目前設定的批核組／執行組處理，會操及典禮組不會在未被明確指派時自動取得物資批核權。**物資申請表 v8.6 簡化**：一次可申請多項物資，每項只填「名稱＋數量（＋單位）」；組別／聯絡電話／申請原因屬整張申請只填一次，電話自動載入登記資料（借十樣嘢唔使填十次）。**攤位計劃書（v8.7，原「攤位物資申請」）對標「2026 攤位總表」＋主題節目組計劃書表單**：分區＋編號＋負責單位由總表選擇；攤位名稱（招牌）、活動內容、「十五五」主題內容、帳篷（3mW x 3mD）／摺枱／摺椅數量、其他要求（圍布／電力等）、負責人姓名／年齡組別／所屬單位／職位／WhatsApp／電郵。**v8.6 起公開可填寫（無需登入）**，未登入須留姓名＋聯絡電話，公眾檢視時負責人電話／電郵自動隱藏。**申請中心「攤位計劃書」入口直接彈表單、取代 Google Form（v8.8）——未登入嘅負責人直接 APP 填寫**。**填完表後資料分兩部分（v8.8）**：① **攤位卡＝借用統計卡（像物資卡）**：「要借什麼」（每項總數＋每攤位明細＋TOTAL）＋**招牌統計**（招牌製作清單）＋計劃書明細（確認／批核）；② **執行手冊 → 攤位總表**（公開）：對標原 2026 攤位總表 OUTPUT，含已聯絡／已回覆／確認出席＋計劃內容＋物資需求，計劃書提交自動填入，TOTAL 總數＋每攤位明細（未提交顯示「未提交」），可匯出 CSV／列印。**主題節目組卡片**另有「攤位資料 (Drive)／攤位總表／借用統計＋招牌」頁籤（DRIVE 內完整攤位資料＋同步）。**MOCK（模擬示範版）示範登入已開放全部管理權限**（v8.7，本地沙盒）。

   👁️ **公眾（未登入）可見範圍（v8.6 收緊）**：膳食只顯示「菜單＋我的訂餐」（訂餐紀錄/統計與列印派發僅供本組總主任以上／批核／執行組）；童心捐贈直接顯示兩張捐贈表（「捐贈登記」多餘頁籤與 QR Code 下載欄只列予籌辦方）；物資（地域借用）、車輛／泊車證、開支申報仍需登入。
5. **部門中心數字口徑（v8.9 根治 ×2）**：主頁「部門管理中心」卡片嘅「N 崗位 · M 人」與進入卡片後嘅崗位數用**同一份** `getGroupOrgNodes()` 計算；去重 key 先正規化（忽略換行／全形空格／半全形括號／人名分隔符差異），所以 Drive 架構圖同步、上傳 JSON/CSV、瀏覽器舊快取三邊都唔會再令崗位／人數／職務大綱內容變一倍。同步與上傳改做 **upsert**（同 key 覆蓋、新 key 先追加）並使用穩定 id（`org_stable_id`）；發現髒快取時 `healStaffOrgCache()` 會自動校正寫回，`data_version` 上調則一次性清走舊 `staff`／`crisis` 快取。回歸測試：`tests/v89_org_chart_duplication_test.js`（69 項，見 `docs/UPDATE_V8_9.md`）。
6. **未登入超清爽版（v8.12）**：**未登入**進入活動後，儀表板只顯示**活動資訊橫幅**（日期／時間／地點／天氣＋簡介＋最新消息＋功能介紹）＋一張**身份卡片**（你好，訪客！公開；初始帳戶＝中文姓名、初始密碼 1234、如需要開戶請找所屬組別的總主任，例子帳號用 MOCK 名「陳子明」），唔再顯示「我的監察」、「公開資料」格線同「登入後解鎖」那堆卡片；**4 項公開資料改為最下方 4 個按鈕**：**公告及溝通・執行手冊・申請中心・童心捐贈**。**登入按鈕全站只有一粒，喺最頂 BAR 右上角**（其餘位置改為文字提示「請按右上角「登入」」）。**登入後全部還原原本設計**：底部導覽列＝執行手冊・申請中心・批核中心（總主任以上）・部門中心；儀表板＝公開資料 → 工作卡片 → 管理工具 → 部門管理中心，「我的監察」亦回到活動資訊橫幅。回歸測試：`tests/ui_layout_check.js`（見 `docs/UPDATE_V8_12.md`）。
7. **2025→2026 執行手冊對標＋上傳方式統一（v9.0）**：① 已對標 2025 執行手冊（行政組／主題節目組／協調組）並分類標亮，見 [`docs/2025_EXEC_MANUAL_COMPARISON.md`](docs/2025_EXEC_MANUAL_COMPARISON.md)；② 場地與活動總覽「地圖」上傳方式＝「遊戲卡」：**PDF／Word／圖片／Drive 連結**（Word 用 mammoth 解析成文字內嵌，PDF 整份內嵌，含版本欄）；③ 「典禮儀式」**所有項目**（RUNDOWN／司儀稿／嘉賓名單／座位表／致辭稿／獲獎名單／嘉賓地圖）新增同款附件上傳（內建 JSON 保留，附件可解析 JSON／Word 文字內嵌、無法解析時整份 PDF 內嵌）；④ 身份＋登出按鈕**只保留最頂 BAR 右上角**，申請中心及我的監察等頁面的重複身份卡／登出按鈕已移除。
8. **最新消息可喺 APP 內改＋與會議預告互換位置（v11.2）**：**「最新消息」升上全站最頂橫幅**（原本「會議預告」個位），**「會議預告」落入活動資訊橫幅**（原本「最新消息」個位，連「發送提醒」掣一齊搬）。最新消息由**執行副主席以上（系統管理員／管理員／顧問／主席／執行副主席）＋秘書處（不論職級）**喺橫幅右邊按「**修改最新消息**」直接改，即時寫入後端 `Events` 表（`news`／`news_updated_by`／`news_updated_at`，後端會自動補建欄位），全部人重新載入即見——**唔使再改 `data/events.json` → commit → push → 等 Vercel 重新部署**。`data/events.json` 嘅 `news` 仍然係「未經 APP 改過」時嘅預設值。⚠️ 需重新部署 `apps-script/Code.gs`（新增 `saveEventNews`）。回歸測試：`tests/v11_2_news_editor_test.js`（見 [`docs/UPDATE_V11_2.md`](docs/UPDATE_V11_2.md)）。
9. **紀念章手動儲存・部門中心統一形態（v12.2）**：① **紀念章 TICK 改為手動「💾 儲存」鍵**——TICK／備註／EXCEL 匯入只存本機，可一口氣 TICK 完整個組別幾十人，工具列實時顯示「有 N 項未儲存」，撳儲存先一次過批次串行寫入（失敗重試 3 次、失敗項保留本機、關頁前提示＋sendBeacon），唔會逐 tick 打後端；② **部門中心統一基本形態**：全部 10 組（含行政組、協調組）都由同一部門頁進入，基本盤＝成員/職務/文件/攤位 4 格＋本組申請統計 5 格（崗位／物資／攤位／車輛／膳食）＋4 張明細表＋**列印本組統計**；各組**特色功能做頂部頁籤**（行政組＝財務/參加旅團/行政文件/票券/紀念章-工作人員/失物認領；協調組＝物資/車輛/膳食批核/場地文件，唔再喺下方另開頁籤列）；③ **最高層系統帳戶唔計入工作人員**（紀念章名單同統計排除）；④ 行政組頁右上角多餘通用「新增」掣已移除；⑤ 紀念章名單 **header 可點擊排序**（姓名／組別／攤位／單位／派發狀態，未派可排最前）。回歸測試：`tests/v121_souvenir_admin_coordin_test.js`（48 項，見 [`docs/UPDATE_V12_1.md`](docs/UPDATE_V12_1.md)）。
10. **部門中心財務一條龍・文件上傳全面升級（v13.0）**：① **部門中心統計升上最頂**（可摺疊，預設展開，數字改為只計本組，新增「💰 開支申報」格：⏳待批／✅已批／❌已拒）；② **4 格資訊（崗位成員／職務大綱／文件／攤位預算）合併為一個整體**，每格可獨立開合、預設收合，仲有「一鍵全展開／全收合」；③ **全部 10 組新增 3 個財務頁籤**——「💰 開支申報」（喺自己部門頁開表，**組別自動帶入本組**，提交即自動入財務紀錄）、 「🗣️ 口頭報價」（同樣自動帶組）、「📖 財務須知」；④ **財務頁頂部通用「新增」掣移除**（財務全部喺下方分頁處理）；⑤ **協調組「場地佈置及文件」**：場地佈置圖真正可上傳（檔案／連結），箱頭紙＋物資借用表格改為固定卡片（已預設齊全，唔使再設定），其他文件一律**必須上傳檔案或填連結**（只打文字會被拒）；⑥ **行政組新增「💰 財務匯總（各部門）」頁籤**：各組申報數＋待批／已批金額一頁睇晒，待批清單一掣批／拒，即時刷新；⑦⑧ **行政文件（管理層）都升級為檔案／連結上傳**（有下載掣）。回歸測試：`tests/v13_dept_center_finance_test.js`（90 項，見 [`docs/UPDATE_V13_0.md`](docs/UPDATE_V13_0.md)）。
11. **不改舊有資料的安全更新架構 (Non-Destructive Schema Update)**：
   - 後端 GAS 採用智慧結構檢驗 (`ensureSheet`)：當你新增功能或測試時，只會自動補齊缺失的欄位，**絕對不會覆蓋或刪除現有的工作表與舊資料**。

---

## 🚀 部署指南 (Deployment Guide)

## ⚠️ 開發守則（必讀）

前端結構（2026-08-27 起）：`index.html` 只剩 HTML 骨架（約 84KB），全部 JS 按功能拆在 `js/` 目錄，**必須按檔名順序載入**（`00-config.js` → `10-app-core.js`（class 主體）→ `20-38` 各功能模組（以 `Object.assign(ScoutEventApp.prototype,{...})` 掛上方法）→ `90-bootstrap.js`（啟動））。**絕對不要再用一次性 Node 腳本＋正則去批量改寫**——這種做法曾兩次弄壞網站（正則截斷方法、整段吞掉仍在用的方法），導致整頁只顯示靜態首頁。

正確做法：
1. **慢慢手改**：小步修改，改邊個 `js/*.js` 就只改嗰個檔；新增方法放在對應模組的 `Object.assign` 區塊內。
2. **每次修改後必跑檢查**：
   ```bash
   # 1) 每個檔案語法檢查
   for f in js/*.js; do node --check "$f" || echo "FAIL: $f"; done
   # 2) 跑全部測試
   for t in tests/*.js; do node "$t"; done
   ```
3. **檢查「呼叫 ↔ 定義」**：改動若刪走方法，先全 `app.方法名(` 有沒有殘留呼叫。
4. 歷史上的一次性補丁腳本（`rewrite_parking.js`、`fix_*.js`、`test*.js` 等）已於 2026-08-27 刪除，只留在 git 歷史內，**不要重跑**。
5. 拆檔經「重組還原恆等式」驗證（所有方法依序串接與原本逐 byte 相同），方法內容零改寫；`js/` 檔案之間的頂層 `const`／`class` 不可重名（瀏覽器共享全域詞法環境，重名即 SyntaxError）。

### 🤖 AI Agent 協作規則（本倉庫主要由 AI Agent 修改）

**開工前：**
1. 先讀本節＋跑一次 `for t in tests/*.js; do node "$t"; done` 確認基線全綠（4 個測試都要 PASS）。
2. 用下面檔案地圖定位目標檔；搵唔到就 `grep -rn "方法名或關鍵字" js/`，或搜 `app.方法名(` 的呼叫處反查定義。

**改動時：**
- 只改與任務相關的 `js/*.js`；新增方法放在該模組 `Object.assign(ScoutEventApp.prototype,{ ... })` 區塊內，**沿用 2 空格縮排＋既有命名風格**。
- 頂層 `const`／`class` 只可加在 `00-config.js` 或 `10-app-core.js`，且**不可與其他檔重名**。
- 小步為主：一次任務改一個功能；跨模組改動分開 commit。
- **絕對禁止**：寫一次性腳本＋正則批量改寫任何檔案；未跑檢查就 commit；「順手」重構無關代碼。

**收工前（必做）：**
```bash
node --check <改過的每個檔>          # 語法
for f in js/*.js; do node --check "$f" || echo "FAIL: $f"; done   # 全檔
for t in tests/*.js; do node "$t"; done   # 測試全綠
```
若改動刪走/改名方法：`grep -rn "app\.舊方法名(" index.html js/` 確認無殘留呼叫。commit message 講明改了什麼、為什麼。

### 📁 js/ 檔案地圖

| 檔案 | 內容 |
|---|---|
| `js/00-config.js` | 常量表（角色/權限卡/儀表板卡定義/LS key/審批路由）＋通用工具（escapeHtml、CSV 解析、登入 ID 遷移等） |
| `js/10-app-core.js` | `class ScoutEventApp` 聲明＋constructor、init、儀表板/卡片渲染/導覽/登入/活動選擇/天氣/通知等核心方法 |
| `js/20-accounts.js` | 開戶＋權限管理（總主任以上） |
| `js/21-activities.js` | 活動卡片模組 v6.8＋文件＋主題章 |
| `js/22-meals.js` | 膳食模組 v6.7 |
| `js/23-sync.js` | 跨裝置同步（所有申請 v7.7） |
| `js/24-supplies.js` | 物資申請＋審批 |
| `js/25-vehicle.js` | 車輛通行證（同物資審批流程） |
| `js/26-monitor-apply.js` | 我的監察＋申請中心 (Apply Hub)＋執行手冊 |
| `js/27-parking.js` | 泊車證申請＋入口檢查清單 |
| `js/28-oral-quotes.js` | 口頭報價登記 |
| `js/30-finance.js` | 財務模組 v6.4 |
| `js/31-staff.js` | 職員/組織架構/聯絡表 |
| `js/32-meetings.js` | 會議功能＋內建議程／會議紀錄 JSON (v8.3) |
| `js/33-users.js` | 用戶管理（可見範圍 v8.3 定案） |
| `js/34-announcements.js` | 公告＋旅團須知（公開） |
| `js/35-ceremony.js` | 典禮儀式＋獲獎名單（公開） |
| `js/36-crisis.js` | 危機處理（含意外事件報告表） |
| `js/37-coordinator.js` | 協調組（物資·車輛·膳食·場地） |
| `js/38-donations.js` | 童心捐贈大行動（物品·食品捐贈） |
| `js/39-lost-found.js` | 失物認領（v11：執行手冊「各類附加資料」＋行政組部門中心，由行政組紀錄；**v11.1：兩種登記情況 ①有失物登記 ②有人要尋找物品，兩者各自出現列表，點入去處理認領＝失物找到物主／尋物者尋回失物，登記及認領時間全部系統自動紀錄**） |
| `js/40-souvenir-stamps.js` | 紀念章派發（v11：行政組＝工作人員 TICK＋備註改名／替假；嘉賓接待組＝嘉賓 TICK，不可改名） |
| `js/90-bootstrap.js` | 啟動（`const app=new ScoutEventApp()`＋Escape 鍵處理） |

### 第一步：設置 Google Sheet 與 Apps Script 後端
1. 在 Google Drive 建立一個新的 **Google 試算表**（例如命名為「活動行政管理總庫」）。
2. 點擊頂部選單的 **擴充功能 (Extensions)** ➔ **Apps Script**。
3. 清除編輯器中的預設程式碼，並將本項目中的 `apps-script/Code.gs` 完整貼上。
4. 點擊上方的 **儲存 (Save)** 按鈕。
5. 執行一次 `initializeSheets` 函數（點擊上方「執行」），授權權限後，它會建立／補齊所有系統工作表，包括 `Approval_Routing`、`Meal_Orders`、`Supply_Requests`、`Vehicle_Passes`、`Parking_Requests` 及 `Finance_Expenses`；此步驟不會重建已刪除的測試申請。
6. **部署為網頁應用程式 (Deploy as Web App)**：
   - 點擊右上角 **部署 (Deploy)** ➔ **新增部署 (New deployment)**。
   - 類型選擇 **網頁應用程式 (Web app)**。
   - 說明填寫 `v1.0`。
   - 執行身分 (Execute as)：**我 (Me)**。
   - 誰可以存取 (Who has access)：**任何人 (Anyone)**。
   - 點擊部署，複製產生的 **網頁應用程式 URL** (`https://script.google.com/macros/s/.../exec`)。

### 第二步：部署前端到 GitHub 與 Vercel
1. 將前端文件（`index.html`）上傳至你的 GitHub 倉庫。
2. 登入 [Vercel](https://vercel.com)，匯入該 GitHub 倉庫。
3. 部署完成後，開啟網頁。
4. 點擊右上角 **「⚙️ 系統設定 / GAS URL」**，將剛才複製的 **Google Apps Script URL** 貼上並儲存。
5. 即完成前後端完整聯動！你可以隨時測試新增會議、提交膳食申請、審批物資等功能，試算表數據實時同步。
