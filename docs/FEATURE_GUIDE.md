# 童軍活動管理系統 — 功能介紹及使用說明書

> 本說明書整理現有系統的功能、三類卡片、各卡片用途、職級權限制度、可延展方向及用戶需要了解的重點。
> 系統定位：**「一個什麼都不知道的人，用此 APP 就可了解整個活動和找到需要的資料。」** 所有公開資料無需登入即可查閱；只有修改才需登入。

---

## 目錄

1. [系統是什麼](#1-系統是什麼)
2. [現有功能：三類卡片](#2-現有功能三類卡片)
3. [主要功能介紹：各卡片功能](#3-主要功能介紹各卡片功能)
4. [各職級權限及權限制度](#4-各職級權限及權限制度)
5. [可延展的功能](#5-可延展的功能)
6. [其他用戶需要了解的地方](#6-其他用戶需要了解的地方)
7. [快速問題 (FAQ)](#7-快速問題-faq)

---

## 1. 系統是什麼

本系統是一個「大型活動行政管理工具」，結合：

- **Google Sheet + Google Apps Script (GAS)** 作為後端，集中管理活動資料。
- **前端網頁 (HTML + 內建 JSON)** 作統一入口，可在手機及電腦使用。
- **多活動接入**：可同時管理港島童軍繽紛日、港島毅行、其他大型活動及演練用示範版。
- **公開查閱**：大部分資料（日程、場地、聯絡、通告、儀式、申請表入口等）無需登入即可閱讀。
- **身份登入**：修改資料、提交申請、批核、統計、開戶等需要依職級／組別登入。

---

## 2. 現有功能：三類卡片

進入活動後，儀表板會根據身分顯示三類卡片；登入後會先多顯示「我的組別」。

| 類型 | 是否需登入 | 內容 | 誰可修改 |
|---|---|---|---|
| **① 我的組別**（登入後） | 需要 | 你所属組別的卡片，排最前並標亮 | 由組別權限決定（總主任以上多可管理） |
| **① 公開資料** | 不用 | 公告、申請中心、日程、場地、組織、主題章、膳食、危機、文件、典禮、捐贈等 | 只讀；修改需對應職級／組別登入 |
| **② 其他組別及工作卡片** | 需要（登入後解鎖） | 其他組別卡片、會議、物資、車輛、財務、監察等 | 預設只讀；依「可看／可管」權限解鎖 |
| **③ 管理工具** | 需要 | 開戶、批核中心、權限管理 | 僅總主任以上可見／使用 |

> 簡單記法：**公開資料唔使登入照睇；自己組優先顯示；其他組只讀；管理工具淨係總主任以上。**

### 首頁活動類別

首頁把活動分為四類：

- **港島童軍繽紛日**：步操檢閱、頒獎典禮與攤位博覽。
- **港島毅行**：步行籌款活動。
- **其他大型活動**：大型露營、培訓及活動。
- **模擬示範版**：完整示範假資料，本地沙盒，改動不會影響正式活動。

---

## 3. 主要功能介紹：各卡片功能

### 3.1 公開類卡片（無需登入可看）

| 卡片 | 主要功能 | 預設可看 | 預設可修改 |
|---|---|---|---|
| 公告及溝通 | 公告、日程表、旅團須知、活動主題章；跨部門溝通 | 公開 | 主任以上可發佈 |
| 申請中心 | 集中所有申請表入口：物資、車輛／泊車證、膳食、口頭報價、開支申報 | 公開 | 各表按權限進入 |
| 場地與活動總覽 | 地圖、攤位總表、遊戲卡、活動項目 | 公開 | 主任／副主席以上可上傳 |
| 組織架構與聯絡 | 樹形組織架構、職員名單、職務大綱、聯絡資料 | 架構公開；電話／Email 需登入 | 行政組／總主任以上可管理 |
| 膳食管理 | 菜單、截止、我的訂餐、統計、列印派發清單 | 公開可見菜單及入口 | 指定膳食執行組主任以上可管理 |
| 危機處理 | 應變指引、急救、保險、危機處理手冊、緊急聯絡 | 公開 | 管理員／副主席以上可更新 |
| 通告及文件 | 通告、指引、表格、活動文件 | 公開 | 管理員／行政總主任以上可上傳 |
| 典禮儀式 | 典禮程序、司儀稿、嘉賓名單、座位表、RUNDOWN、獲獎名單 | 公開 | 管理員／副主席以上可更新 |
| 童心捐贈大行動 | 物品捐贈、食品捐贈、QR Code 登記、統計總表 | 公開 | 服務及發展組可管理 |

### 3.2 工作類／組別類卡片（登入後依權限顯示）

| 卡片 | 主要功能 | 預設可看 | 預設可修改 |
|---|---|---|---|
| 會議卡片 | 籌委會議議程、會議紀錄、附件、Drive 資料夾即時同步；內建 JSON 摘要即睇 | 主任以上可看 | 管理員可新增會議 |
| 物資申請 | 物資借用申請、批核、通知、截止 | 總主任以上可申請／批核 | 登入成員可申請；批核按組別路由 |
| 車輛通行證（含泊車證） | 車牌／司機／用途申請，沿用車輛批核及執行路由 | 協調組等按路由 | 協調組可管理 |
| 口頭報價登記 | 商戶／項目／金額登記 | 行政組及執行副主席以上可查看 | 總主任以上可登記 |
| 財務管理 | 財務指引、預算、開支申報、結算按銀碼批核 | 行政組及按路由 | 副主席以上批核 |
| 行政組 | 通告、報名、膳食安排、參加旅團、財務 | 登入可看 | 行政組可管理 |
| 協調組 | 物資、車輛、膳食及場地管理 | 登入可看 | 協調組可管理 |
| 顧問團 | 政策指導、整體監察及顧問資料 | 登入可看 | 顧問／管理員可管理 |
| 主席及執行副主席 | 主席、執行副主席及整體統籌資料 | 登入可看 | 主席／執行副主席可管理 |
| 會操及典禮組 | 步操檢閱、典禮程序、本組內容 | 登入可看 | 本組總主任以上可管理 |
| 主題節目組 | 主題節目、攤位構思、本組內容 | 登入可看 | 本組總主任以上可管理 |
| 品牌推廣組 | 宣傳、設計、本組內容 | 登入可看 | 本組總主任以上可管理 |
| 嘉賓接待組 | 嘉賓、接待安排、本組內容 | 登入可看 | 本組總主任以上可管理 |
| 服務及發展組 | 服務、發展、本組內容、捐贈 | 登入可看 | 本組總主任以上可管理 |
| 秘書處 | 地域秘書處受薪職員、本組內容 | 登入可看 | 秘書處管理員可管理 |

### 3.3 管理／監察類卡片

| 卡片 | 主要功能 | 誰可用 |
|---|---|---|
| 我的監察 | 查看自己＋下級所有申請批核：膳食、物資、車輛、開支、報價 | 登入成員；只可看自己有權看的 |
| 批核中心 | 各負責組按範疇處理膳食、物資、車輛及財務待批項目 | 指定批核／執行組總主任以上 |
| 批核權限表 | 設定各申請範疇的「批核組」及「執行／最後名單組」（多選） | L1 或以上（管理員／顧問／主席／執行副主席） |
| 開戶 | 為本組逐個開戶；批量開戶 (CSV／JSON) | 總主任以上可開戶；批量開戶管理員專用 |
| 權限管理 | 一次過改密碼、組別、角色及卡片「可看／可管」權限 | 上級授權給下級 |

---

## 4. 各職級權限及權限制度

### 4.1 職級與預設權限總表

| 層級 | 職級 | 代碼 | 預設權限 | 用戶管理可視範圍 |
|---|---|---|---|---|
| L0 | 超級管理員（不顯示） | super_admin | 僅後台／系統授權管理 | 全部人（不向其他職級顯示） |
| L1 | 管理員 | admin | 全部可看、全部可管（行政／秘書處常用） | 可見全部人 |
| L1 | 顧問 | advisor | 全部可看、全部可管（顧問團） | 可見全部人 |
| L1 | 主席 | chairperson | 全部可看、全部可管 | 可見全部人 |
| L2 | 執行副主席 | executive_vice_chairperson | 大部分可看可管（高層統籌） | 可見全部人 |
| L3 | 副主席 | vice_chairperson | 可更新日程／主題章／儀式／危機等；管理本組 | 只睇自己組全部成員 |
| L4 | 總主任 | general_director | 可管理本組、申請、批核、開戶、報價 | 自己組，不包括副主席及以上 |
| L5 | 主任 | director | 可管理本組內容、上傳活動、申請 | 自己組普通工作人員 |
| L6 | 工作人員 | staff | 可登入、提交申請、查看自己組 | 原則上無權管理用戶 |
| L7 | 公開 | public | 只可看公開資料（無需登入） | — |

> 註：以上為「預設」權限。系統支援上級透過「權限管理」把更細的「可看／可管」授權給下級（`perm_see`／`perm_edit`）。

### 4.2 權限制度運作方式

1. **卡片可見度（minLevel / groups）**
   - `minLevel = 0`：公開，所有人可見。
   - `minLevel > 0`：需登入且職級達標，或屬於卡片指定組別。
2. **卡片修改權（editLevel / editGroups）**
   - 達到 `editLevel` 或屬於 `editGroups` 才會顯示「可修改」；否則顯示「只讀」。
   - 各模組另有實作權限（如訂餐、物資、財務、上傳），會以卡片內按鈕及提示為準。
3. **角色授權（perm_see / perm_edit）**
   - 若上級已為某用戶設定權限清單，系統會以該清單為準。
   - 管理層（管理員／顧問／主席／執行副主席）預設全看全管。
4. **組別卡片**
   - 登入後仍可只讀查閱其他組別卡片，方便跨組協作。
   - 自己的組別會排最前並標亮。
5. **用戶可見範圍**
   - 管理員／執副／主席／顧問＝可見全部人。
   - 副主席＝只睇自己組。
   - 總主任＝自己組，不包括副主席及以上。
   - 主任＝自己組普通工作人員。

### 4.3 開戶權限

- 前端開戶卡片只提供：**工作人員／主任／總主任** 三級。
- **副主席／執行副主席／主席／顧問**：由管理員另行處理。
- **管理者（管理員）帳戶**：可由管理員另開（如秘書處）。
- 籌委會架構 54 人已全部預先開戶（見 `data/committee_accounts.csv`），冇資料嘅可於「用戶管理／開戶表／批量 CSV」補。
- 工作人員（數量多、名單後補）由各組自行開戶。
- 組別下拉（開戶／登記）不包含「顧問團」及「主席及執行副主席」兩組。

### 4.4 申請批核路由（重要）

每類申請均可獨立設定「**批核組**」及「**執行／最後名單組**」，L1 或以上（管理員／顧問／主席／執行副主席）可在「批核權限表」按鈕多選並隨時修改。

| 申請範疇 | 預設批核組 | 預設執行／最後名單組 |
|---|---|---|
| 物資申請 | 協調組 | 協調組 |
| 車位／車輛（含泊車證） | 協調組 | 協調組 |
| 膳食申請 | 行政組 | 協調組 |
| 財務申請／開支 | 行政組 | 行政組 |

- 低於總主任的申請，會先由申請人**本組總主任以上**確認，再交由目前設定的批核組處理。
- 只有設定的執行組可查看最後名單、列印入口檢查清單（例如泊車證）。
- 會操及典禮組不會在未被明確指派時自動取得物資批核權。

---

## 5. 可延展的功能

本系統已預留以下擴充方向，供日後發展：

1. **多活動接入**：切換不同年度／類別的活動，甚至擴展到其他地域活動。
2. **通用化**：將童軍專屬內容（警察學院、步操檢閱等）參數化，變成「活動類型可配置」，供其他大型活動重用。
3. **動態批核路由**：已可多選「批核組／執行組」，未來可再加入條件式路由（按銀碼、組別、日期等）。
4. **權限授權**：已支援 `perm_see / perm_edit` 授權，未來可做到「每一張卡片、每一個動作」皆可細粒度授權。
5. **統計儀表板**：現有各組訂餐統計、物資統計、財務統計，可擴充為跨活動匯總、圖表及匯出。
6. **文件／Drive 整合**：會議 Drive 即時連通、文件上傳至指定資料夾，可擴充為「自動同步、版本記錄、全文檢索」。
7. **CSV／批量作業**：已有批量開戶、批量上傳，可擴充為批量匯入人員、物資、財務、攤位、日程。
8. **全域搜尋**：現有全域搜尋可擴充為跨卡片全文檢索（包括文件內容、申請紀錄）。
9. **捐贈管理**：已預留 QR Code 登記及統計總表。
10. **離線／快取**：資料以 `localStorage` 快取，可擴充為更完善的離線同步。
11. **後端結構自動更新**：GAS 使用 `ensureSheet` 智慧結構檢驗，新增功能時只補齊缺失欄位，不會覆蓋舊資料，方便持續升級。

---

## 6. 其他用戶需要了解的地方

### 6.1 公開查閱 vs 登入修改
- **無需登入**即可查看「公開資料」卡片。
- **修改資料、提交申請、批核、開戶**必須登入。
- 登入狀態會保存在瀏覽器 `localStorage`，**一次登入永久有效**，直到主動登出。

### 6.2 如何登入／開戶
- 帳號＝**中文姓名**（例如「朱家聰」）。
- 初始密碼：**1234**（首次登入後建議立即改密碼）。
- 如需要開戶，請找**所屬組別的總主任**；管理層帳戶由管理員處理。

### 6.3 模擬示範版（Mock）
- 首頁的「模擬示範版」是**本地沙盒**，含全部示範假資料。
- 在此登入只會使用示範帳戶，所有改動只存在瀏覽器，**不會影響正式活動資料**。
- 適合會議簡介、培訓及測試使用。

### 6.4 資料來源及同步
- 正式活動：資料可保存在 Google Sheet（GAS 後端），會實時同步。
- 內建資料：部分內容（活動、會議紀錄、組織架構等）來自 `data/*.json`，隨 APP 一齊發佈。
- 管理員可於「系統設定 / GAS URL」設定後端網址；未設定時整站以本地資料運行。
- 會議卡片讀取公開 Drive 資料夾，檔案需設為「知道連結的任何人可查看」。

### 6.5 私隱與安全
- 公開資料刻意公開（讓任何人快速了解活動）；聯絡電話／Email 需登入才可查看／匯出。
- 活動密碼、登入密碼會儲存於瀏覽器；敏感帳密不會在網頁源碼顯示。
- 「用戶管理／權限管理」及「批核權限表」只開放給有權限的職級。

### 6.6 手機使用
- APP 已針對手機優化；手機底部有「首頁／會議／成員／開戶」快捷列。
- 表格在手機上會以「標籤＋值」方式顯示，方便閱讀。

### 6.7 遇到問題
- 睇唔到卡片：可能是卡片屬登入後模組，或你的職級／組別未達標，或該活動未有資料。
- 要改資料冇按鈕：代表你的權限只有「只讀」，請向該卡片的可修改職級／上級反映。
- 資料未更新：會議 Drive 檔案請重新入卡片或按「讀取最新」；其他資料可重新載入頁面。

---

## 7. 快速問題 (FAQ)

**Q：不登入可以看到什麼？**
A：看到「公開資料」區的卡片（公告、申請中心、日程、場地、組織、主題章、膳食、危機、文件、典禮、捐贈等），全部只讀。

**Q：登入後有咩唔同？**
A：會先顯示並標亮「我的組別」，再顯示「公開資料 → 其他組別及工作卡片 → 管理工具」；依職級／組別解鎖「可修改」及「可看」。

**Q：點解我冇得修改某張卡？**
A：表示你冇該卡的修改權限（只讀）。可睇該卡顯示的「可修改：誰」提示，或向該卡負責人／上級查詢。

**Q：我可以幫下屬開戶嗎？**
A：總主任以上可為本組開「工作人員／主任／總主任」；批量開戶由管理員使用。

**Q：申請咗膳食／物資／泊車證，點知批咗未？**
A：登入後用「我的監察」，一頁看齊自己＋下級的申請進度；亦可在申請中心／卡片上看狀態及通知。

**Q：呢個系統係咪只係「2026 港島童軍繽紛日」用？**
A：唔係。系統支援多活動接入，內建分類包括港島童軍繽紛日、港島毅行、其他大型活動，亦容許新增活動。

---

*文件版本：v1.0（2026-08-25）*
*相關文件：`docs/CARD_CLASSIFICATION_V8.md`、`docs/ACCOUNT_SETUP.md`、`docs/BULK_ONBOARD.md`、`README.md`*
