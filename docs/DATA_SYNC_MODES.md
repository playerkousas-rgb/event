# 🔄 資料更新模式 (Data Sync Modes) — 給各組的更新指引

> 目的：把「資料更新」交回各組負責人，系統架構師（你）只需專心搞好系統，不用逐項監控。
> 核心原則：**檔案歸各組、APP 直接讀**（內建式，不跳轉 Drive）。

## 先釐清一件事

**你在 Drive 更新檔案 ≠ APP 自動更新。** Drive 不會主動把內容「推」進 APP。

APP 要有最新資料，只有兩種途徑：

| 模式 | 做法 | 誰更新 | APP 顯示 |
|---|---|---|---|
| **A. Drive 直接讀（內建式，推薦）** | 檔案存成**原生 Google 試算表**，APP 每次打開用 `export=csv` 直接抓最新 | 各組在 Google Sheet 改即可 | 即時、不跳轉 ✅ |
| **B. 上傳 Excel → 寫後端** | 副主席上傳 .xlsx，APP 用 SheetJS 轉 CSV/JSON → 寫入 ISD 後端 Sheet | 副主席按「上傳」 | 上傳後更新 |

## 模式 A（推薦）：把結構化資料做成「原生 Google 試算表」

**適合**：攤位總表、工作人員名單（Org Chart）、預算（Budget）、參加旅團名單等**表格類資料**。

1. 在 Google Drive 開啟該 .xlsx 檔案。
2. 選單「**檔案 → 另存為 Google 試算表**」（或直接上傳時勾選「轉換為 Google 文件格式」）。
3. 把新產生的試算表 ID 填入該卡片對應的 `xxx_source` 設定。
4. 之後各組在該試算表更新，APP「同步最新」即直接讀取。

✅ 好處：各組照舊用 Google Sheet 工作，APP 完全同步、零跳轉、零上傳。

## 模式 B（備用）：不便轉 Sheet 時，副主席上傳 Excel

**適合**：必須保留 .xlsx 格式（複雜公式/格式）的檔案，或不想轉換的情況。

1. 節目組副主席（或該卡片有權限者）在 APP 點「⬆️ 上傳 Excel → 寫入後端」。
2. APP 用 SheetJS 解析 → 轉成 CSV/JSON。
3. 透過 GAS `saveBooths`（或其他模組的批次寫入）寫入 ISD 後端 Sheet。
4. 所有人在 APP 內看到最新資料。

✅ 好處：不需要動 Drive，直接由負責人上傳即同步後端。

## 文件類（PDF / Word 通告、指引）

這些是「閱讀用」參考文件，**不需要同步**，沿用「Drive 嵌入預覽 + 擇要摘要」即可：
- 每份文件有「擇要」摘要（一眼知道內容）+「嵌入預覽」+「下載」+「詳細」。
- 各組負責人更新 Drive 檔案後，APP 的連結指向同一檔案，用戶點開就是最新。

## 已接上「資料源 + 同步」的表格類卡片

以下卡片都已套用同一套機制（「🔄 同步最新」直接讀 Drive ＋「⬆️ 上傳 Excel」寫後端）：

| 卡片 | 資料源 (Drive 檔) | 模式 | 負責組別 |
|---|---|---|---|
| 攤位總表 | ISD2026 攤位資料.xlsx | A/B | 節目組副主席 |
| 預算明細 | ISD2026 Budget.xlsx | A/B | 行政組／財務 |
| 工作人員名單 | Org Chart and Contact List（已是原生 Sheet） | A/B | 行政組 |
| 日程表 | 待秘書處提供 Google Sheet | A/B | 秘書處 |
| 參加旅團名單 | 待行政組提供 Google Sheet | A/B | 行政組 |

> ⚠️ 工作人員 Org Chart 是「架構圖」，不是乾淨表格，自動同步會解析失敗。建議行政組在該 Sheet **另加一個「聯絡名單」扁平分頁**（見下方範本），即可一鍵同步。

### 「聯絡名單」扁平分頁範本（行政組照抄）

在 Org Chart 試算表**新增一個分頁**，第一列（表頭）照填，之後每列一位工作人員：

```
name,role_title,group_name,contact,job_desc,email,squad
朱家聰,主席,籌委會,92222222,全域統籌與會議主持,chair@isd.local,
袁可秀,執行副主席,籌委會,93333333,行政、秘書處與財政審批,execvp@isd.local,
張佳良,副主席（會操及典禮）,會操及典禮組,94444444,會操流程與步操後備日,,
周恒晉,副主席（主題節目）,主題節目組,95555555,攤位與遊戲統籌,,
```

APP 亦提供「下載範本 CSV」按鈕（工作人員卡片 → 名單及聯絡 → 下載範本），可直接拿範本檔照填。

> 其他表格同樣原則：**第一列放表頭，之後每列一筆資料**。表頭關鍵字要能對應（攤位用「攤位編號/攤位名稱/組別…」，預算用「組別/項目/預算/實際…」），APP 就會自動拆解、排列成內建卡片顯示，不會顯示醜醜的一整張 Sheet。

## 各組分工建議（草案，待你與各組確認）

| 卡片 / 資料 | 建議模式 | 負責組別 |
|---|---|---|
| 攤位總表 | A（Google Sheet 直接讀） | 節目組副主席 |
| 工作人員名單 Org Chart | A（另設扁平「聯絡名單」分頁） | 行政組 |
| 預算 Budget | A（另存為 Google 試算表） | 行政組／財務 |
| 財務指引 + 附件 | 文件類（嵌入預覽） | 行政組 |
| 通告 (sp12 等) | 文件類（嵌入預覽） | 秘書處 |
| 場地佈置圖／泊車證 | 文件類（嵌入預覽） | 協調組 |
| 膳食／物資／車輛／開支 | 表單類（內建表單 + 批核） | 各申請組 + 批核人 |

## 表單／批核類（你的第 4 點）— 已是內建機制

這些**不用**走 Drive 同步，而是「卡片內建表單 → 寫入主 Sheet → 批核」：

- **膳食**：各組填訂餐 → 膳食組批核 → 申請人看「我的監察」查自己訂了什麼、待批/已批。
- **物資**：各組申請 → 協調組批核 → 通知申請人。
- **車輛通行證**：申請車牌/司機 → 協調副主席批核 → 通知。
- **財務開支**：填表+上傳單據 → 按銀碼門檻批核 → 結算總表。

申請人登入後在「我的監察」卡片即可看到自己項目的狀態（待批/已批）與內容。

## 系統架構師（你）的角色

- 你只管**架構、權限、卡片分類**，及為每個資料源配好 `xxx_source`（Drive 檔案/試算表 ID）。
- 各組負責人自行更新自己的資料，APP 直接讀或上傳寫入，**你不用監控每一筆**。

## 📢 轉發用「一句話更新指引」（發給各組）

| 組別 | 一句話指引 |
|---|---|
| 節目組副主席 | 攤位總表在 Drive 的「ISD2026 攤位資料」改完，APP 一開就自動同步；若想手動可進卡片按「同步最新」。 |
| 行政組 | 在 Org Chart 試算表新增一個「聯絡名單」分頁照範本填，APP 開就會讀到最新名單。 |
| 行政組／財務 | 把「ISD2026 Budget」另存為 Google 試算表後照常更新，APP 開就同步預算。 |
| 秘書處 | 通告／政策等 PDF 不常變，要更新就重新上傳即可（有擇要+預覽）。 |
| 協調組 | 場地圖／泊車證等 PDF 用「嵌入預覽」，要更新就重新上傳。 |
| 各申請組 | 膳食／物資／車輛／開支直接在卡片內填表，批核後在「我的監察」看狀態。 |

COPYRIGHT 2026 Scout System
