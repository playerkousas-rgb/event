# 👤 帳戶開設、密碼管理與權限授權

> 目的：兩種方式開戶（後端開戶表 + 前端開戶卡片），預設密碼 1234，登入後自行改密碼；並提供「權限管理」卡片，讓上級把「看/管」權限授權給下級。

## 一、開戶表（Account_Setup）

`initializeSheets()` 會自動生成一個名為 **`Account_Setup`** 的工作表，欄位如下（第 1 列為表頭）：

| 欄位 | 說明 |
|---|---|
| `name` | 名字（必填） |
| `role` | 職位層級（下拉選：主席／顧問／管理員／副主席／總主任／主任／工作人員） |
| `job_title` | 職稱（例：副主席（主題節目）） |
| `group_name` | 組別（例：主題節目組） |
| `user_id` | 登入帳號（可留空＝自動用中文姓名，同名自動加 -2/-3，並回寫到本表） |
| `email` | 電郵（選填，登入時可當帳號用） |
| `contact` | 電話（選填） |

## 二、如何開戶

1. 在 Google Sheet 打開 `Account_Setup`，從第 2 行起逐行填寫。
2. 選單列「**童軍活動管理 → 開戶（同步 Account_Setup → Users）**」。
3. 完成後每個新帳戶：
   - 登入帳號 = 中文姓名（`user_id` 留空則自動用名字）
   - **預設密碼 = 1234**
   - 已存在的帳戶只更新名字/職銜/組別，**不會覆蓋其已修改的密碼**。

> 也可透過前端 POST `action: syncAccountsFromSetup`（含 api_key）觸發同步。

## 三、用戶自行改密碼

1. 用戶登入後，右上角會出現「**修改密碼**」按鈕。
2. 輸入舊密碼 → 新密碼（至少 4 字元）→ 確認，即更新。
3. 後端 `changePassword` 驗證舊密碼後，把新密碼雜湊寫入 `Users.password_hash`。

## 四、角色權限對照

| 職位層級（下拉） | 系統角色 | 層級數值 | 說明 |
|---|---|---|---|
| 主席 | `chairperson` | 80 | 管理層 |
| 顧問 | `advisor` | 80 | 管理層 |
| 管理員 | `admin` | 80 | 管理層 |
| **執行副主席** | `executive_vice_chairperson` | 70 | 管理層，統管整個活動（比一般副主席高半級） |
| 副主席 | `vice_chairperson` | 60 | 各組別 |
| 總主任 | `general_director` | 40 | 各組別 |
| 主任 | `director` | 30 | 各組別 |
| 工作人員 | `staff` | 20 | |

### 執行副主席（統管整個活動）
- 層級 70，介乎主席(80)與副主席(60)之間。
- 默認**全看＋全管**所有卡片（跨組），與管理層同級。
- 可跨組開戶、可授權給所有下級（跨組）。
- 具備副主席級（層級 ≥ 60）的所有批核權（膳食、物資、財務等）。


>
> ⚠️ **v7.8 起**：**前端開戶卡片只提供 工作人員／主任／總主任 三級**（副主席及以上職級已確定，由管理員直接處理）。副主席／執行副主席／主席／顧問等職級只經以下途徑設定：① 開戶表（Account_Setup 直接填 role）② 批量 CSV／JSON（管理員專用）③ 「用戶管理」頁編輯。組別下拉（開戶/登記）已移除「顧問團」「主席及執行副主席」兩組；**部門管理中心仍顯示全部 10 組**（該兩組只作查看/統計）。
>
> 🎯 **籌委會架構帳戶已全部預先開設（ISD 2026，共 54 人）**：由組織架構圖自動生成（顧問 2、主席 1、執行副主席 1、副主席 7、總主任 9、主任 29、秘書處管理員 5）。名單見 `data/isd_2026.json` 的 `users`（本地／Mock 模式自動載入）及 `data/committee_accounts.csv`（正式後端批量開戶用，管理員於「開戶 → 批量開戶 → 上傳已填 CSV」直接匯入）。帳號＝中文姓名，預設密碼 `1234`。差幾個未齊嘅由管理員補充；一般工作人員（數量多、名單後補）由各組自行開戶。
>
> 🏛️ **秘書處（地域受薪職員，全為管理員，共 5 人）**：
> | 姓名 | 職稱 | 電話 |
> |---|---|---|
> | 譚健祥 (Figo) | 執行幹事 (Scout Executive) | 2835 7711 |
> | 何家耀 (Oscar) | 助理執行幹事 (Assistant Scout Executive) | 2835 7712 |
> | 陸詠德 (Fiona) | 活動幹事 (Programme Officer) | 2835 7713 |
> | 鄧倩姸 (Cindy) | 發展幹事 (Development Officer) | 2835 7714 |
> | 曾凱瑩 (Sandy) | 訓練幹事 (Training Officer) | 2835 7715 |
>
> 秘書處為部門管理中心 10 組之一，開戶卡片可選「管理員」職級，為秘書處同事開戶（其他職級只可開 工作人員／主任／總主任）。

## 五、前端開戶卡片（總主任以上）

- APP 內新增「**開戶**」卡片，總主任或以上可見。
- 只能為**自己組別**開戶（管理員可跨組）。
- 角色下拉只顯示「層級 ≤ 自己」的角色。
- 預設密碼 1234，登入後可自行修改（右上「修改密碼」）。
- 後端 `createAccount` 會做輕量防呆（總主任以上、同組）。

## 六、權限管理卡片（看／管授權）

- APP 內新增「**權限管理**」卡片，總主任或以上可見。
- 每張內容卡片有兩種權限：**可看到（perm_see）** 與 **可編輯（perm_edit）**。
- 上級只能把「**自己有的**」權利授權給下級：
  - 只能授權給「同組、層級比自己低」的下級。
  - 授權卡片清單 = 自己「可看」的卡片子集；「可管」⊆「可看」。
- 例子：
  - 行政副主席（看管 ABCDE）→ 授行政總主任「看 ABC、管 AB」；行政總主任再往下，就只能授出 ABC 的看、AB 的管。
  - 運作總主任（看管 DE）→ 他之下就只有 DE 的權。
- 後端把授權存入 `Users.perm_see` / `Users.perm_edit`（JSON 陣列）；登入後生效。

### 權限判定規則
- 內容卡片：有 `perm_see` / `perm_edit` 時**以授權為準**（無則回退角色/組別預設；管理層 level ≥ 70 默認全看全管，含執行副主席）。
- 管理類卡片（開戶、權限、用戶批量、批核中心）仍按角色層級。

## 七、關於「管理權 vs 批核權」的框架分工

- **內容「可編輯」權**（perm_edit）：決定誰能改該卡片的內容（攤位、名單、預算等），由上級往下授權。
- **批核權**（物資、車輛／泊車、膳食、財務）：批核中心只供總主任以上查看。低於總主任提交的每項申請先由申請人本組總主任以上確認；總主任以上親自提交可跳過本組確認。其後才交 `Approval_Routing` 目前設定的批核組，批准後由設定的執行／最後名單組跟進。
- 每類申請的批核組與執行組均可多選，並可由管理層在前端以組別按鈕隨時修改；不是下拉選單。膳食預設為行政組批核、協調組執行。
- 因此「權限卡片」的看/管授權，與「批核權」是兩層：
  1. 卡片層（perm_see / perm_edit）—— 框架化、上級自助授權；
  2. 批核層 —— 管理員手動指定，或批核權限人自行組內指派。

## 八、批核權限表（Approval_Permissions）＋ 前端批核頁兩種檢視

### 後端「批核權限表」
- `initializeSheets()` 會生成 **`Approval_Permissions`** 工作表，欄位：
  `user_id, name, group_name, supplies, vehicle, meals, finance`
- 4 個批核範疇：
  - `supplies` = 物資批核
  - `vehicle` = 車位/車輛批核
  - `meals` = 膳食批核
  - `finance` = 財務批核
- 有權的欄位填「**Y**」或「是」，無權留空。
- 管理層（主席/顧問/管理員/**執行副主席**）預設全範疇，**不需列明**。
- 修改後，前端「批核」分頁點「重新整理」即生效（或重新開啟 APP）。

### 動態路由表（Approval_Routing）
- `initializeSheets()` 會生成 `Approval_Routing`：每個活動、每類申請一列。
- `approver_groups` 與 `executor_groups` 以 JSON 多選陣列儲存；前端用組別按鈕選取，可同時選多組並可日後修改。
- 預設：物資／車輛為協調組；膳食為行政組批核、協調組執行；財務為行政組。泊車證沿用 `vehicle` 路由。

### 前端分頁（兩個獨立分頁）
1. **「批核」分頁**：
   - 「待確認／批核」顯示膳食、物資、車輛／泊車及財務。申請人本組總主任以上先看到「本組確認」，完成後設定的批核組才看到批准／拒絕。
   - 「執行／最後名單」只向目前 `executor_groups` 內的組別顯示已批核項目；多個執行組可同時取得同一名單。
   - 「我的申請」讓登入者查看訂餐、物資、車輛／泊車與開支的本組確認、最終批核及拒絕狀態。
2. **「批核權限表」分頁**：檢視「誰有哪個範疇的批核權」，有兩種檢視：
   - **橫看（人 → 頁）**：表格每一行＝一位批核人，每一欄＝一個批核範疇，用**顏色晶片＋「有權/無權」文字**顯示。
   - **直看（頁 → 人）**：每個批核範疇一列，列出「有權」的批核人名單（同樣顏色＋文字分類）。

### 批核權判定（canApproveArea）
1. 管理層保留全範疇監察／代批權。
2. 其他人須至少為總主任，且所屬組別在該類申請的 `approver_groups`。
3. 如 `Approval_Permissions` 有該人的紀錄，再以該範疇的 Y／否收窄個人權限；未列明則由獲指派組別的總主任以上處理。
4. `executor_groups` 獨立判斷最後名單、列印及匯出權，不會因擁有批核權而自動取得。

> 管理員隨時可在後端／用戶管理直接改任何人的權限，框架只是為了「減少手動」。

COPYRIGHT 2026 Scout System
